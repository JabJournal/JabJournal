<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ── Notifications ─────────────────────────────────────────────────── -->
    <!-- Android 13+: required to actually post notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Wake the device to fire scheduled alarms -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Re-register scheduled alarms after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Vibrate when notification fires -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- ── Exact alarms (Android 12+) ────────────────────────────────────── -->
    <!-- USE_EXACT_ALARM is the recommended permission for medication reminder /
         alarm clock-style apps. It's auto-granted on install (no user dialog)
         and is what makes scheduled doses actually fire on time. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- Fallback for Android 12 (API 31) which doesn't support USE_EXACT_ALARM.
         User must grant this in Special App Access. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- ── Battery optimization ──────────────────────────────────────────── -->
    <!-- Lets us prompt the user to exempt this app from Doze / App Standby,
         which can otherwise delay or drop scheduled alarms. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- ── Foreground service ───────────────────────────────────────────── -->
    <!-- A persistent, low-priority foreground service that keeps the app
         process alive so scheduled medication reminders fire reliably. The
         user can disable it from the Schedules screen if they don't want the
         persistent notification. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

    <application
        android:label="JabJournal"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- ── Icon variant aliases ──────────────────────────────────────── -->
        <!-- Each alias targets MainActivity. Only one alias (or MainActivity
             itself for Ocean) should be ENABLED at a time — the others are
             DISABLED so only one launcher shortcut is visible. Ocean is the
             default: MainActivity is enabled, all aliases start disabled.   -->
        <activity-alias
            android:name=".MainActivityForest"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_forest"
            android:roundIcon="@mipmap/ic_launcher_forest"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".MainActivityAmethyst"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_amethyst"
            android:roundIcon="@mipmap/ic_launcher_amethyst"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".MainActivitySlate"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_slate"
            android:roundIcon="@mipmap/ic_launcher_slate"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ── flutter_foreground_task service ─────────────────────────── -->
        <!-- Keeps the app process alive so scheduled medication reminders
             fire on time. Required on Android 14+ to declare a specific
             foregroundServiceType; "specialUse" is appropriate for
             medication reminder apps. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="specialUse"
            android:exported="false"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the app running in the background so scheduled medication and peptide dose reminders fire on time. The persistent notification can be silenced by the user from the app settings."/>
        </service>

        <!-- ── flutter_local_notifications receivers ───────────────────── -->
        <!-- These MUST be declared in the app manifest as of plugin v21 —
             the plugin's own AndroidManifest.xml no longer declares them,
             so without these the AlarmManager broadcasts fire into a void
             and scheduled notifications never appear. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
